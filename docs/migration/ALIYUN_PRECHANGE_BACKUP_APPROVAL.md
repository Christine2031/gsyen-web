# 阿里云生产变更前备份审批单

更新日期：2026-08-27（Asia/Shanghai）  
当前状态：**未批准，禁止创建快照、安装工具或写入生产 ECS**

## 精确目标

| 项目 | 值 |
|---|---|
| 地域 | 华北 2（北京），`cn-beijing` |
| ECS | `i-2zeewhay0farxq8lucrd` |
| 系统盘 | `d-2ze9t48edu0hojhpho4q` |
| 云盘类型/容量 | ESSD Entry，100 GiB |
| 当前快照 | 0 |
| 当前文件备份 | ECS 文件备份基础版已保护 100 GiB；历史跨 4 页，最新任务仍按日运行 |

上述值由 2026-08-27 阿里云 ECS/云备份控制台只读详情复核；未点击创建、购买、恢复或
策略按钮。最新完成恢复点为 `2026-08-27 06:55:17`，到期日为 2026-09-26；控制台
“上次恢复时间”仍为 `-`。2026-08-26 曾精确观察到 18 个版本，本次分页界面未重新把
总数解释为新的精确数量。因此现有文件备份不能替代云盘快照、离线副本和恢复演练。

## 费用证据与上限

[阿里云 ECS 实时定价页](https://ecs-buy.aliyun.com/price) 当前发布的华北 2 标准
快照价格数据为 `0.148 元/GB/月`。官方 FAQ 同时说明快照没有免费额度，创建后按
占用容量计费，并建议在有操作风险的关键系统修改前创建快照：
[快照管理与使用 FAQ](https://help.aliyun.com/zh/ecs/data-protection-and-recovery-faqs)。

按整块 100 GiB 云盘计算的保守上限：

```text
100 GB × 0.148 元/GB/月 = 14.80 元/月
```

实际账单取决于快照实际占用和保留时长。此审批不包含跨地域复制、自动快照策略、
OSS 资源包、KMS 新密钥或任何其他付费产品。

## 建议批准范围

1. 创建一个手工标准快照，名称含 `pre-gsyen-halfsphere-migration` 和 UTC 时间；
2. 不启用跨地域复制，不绑定自动快照策略；
3. 在迁移和回滚观察期内保留，按最高 `14.80 元/月` 预算；
4. 快照删除不包含在本次批准中，届时针对精确 snapshot ID 再次请求确认；
5. 快照完成且状态可用后，才允许进行会写入生产 ECS 的迁移操作；
6. 文件级备份优先经 SSH 加密传输到本机离线目录，不复用未知归属的现有 OSS
   Bucket；如果必须新增 OSS/备份服务，另行列价并审批。

## 快照后的文件级备份门槛

- 已有 ECS 文件备份降低了“完全没有恢复点”的风险，但全部历史版本均未验证恢复，
  控制台免费额度已用 100 GiB、剩余 0 GiB，实际账单也尚未核对；
- 先只读确定 systemd、Caddy、Stalwart、应用、配置、数据和日志的精确路径/大小；
- 不把 Secret 输出到终端或日志；归档在传输过程中加密，离线副本权限 `0600`；
- 记录文件清单、权限、owner、mtime、archive SHA-256 和恢复命令；
- 文件归档不得跨越到无关供应链应用，除非为了 ECS 整体恢复而单独列为系统配置；
- 不在同一块系统盘上把本地归档冒充灾备；
- 当前同盘应用归档有 326 个条目、SHA-256
  `529a7b95297438a9ddb0ff1c56ff20a942190586dc2a77e86306cae8ae02a0d7`，但未加密、
  不含受保护 config/Secret 且从未恢复；它只作旧应用副本证据。
- 现有 target-layout 备份脚本要求 immutable `current/releases`，不能直接备份当前 legacy
  布局；快照后还须先通过独立 legacy allowlist 备份脚本的本地审查，禁止在生产临时拼命令。
- legacy 的精确 allowlist、加密 manifest、容量/锁、一次性 approval marker 与隔离恢复合同见
  [现有 ECS legacy 状态的变更前备份合同](./LEGACY_ECS_PRECHANGE_BACKUP_DESIGN.md)；当前仍缺
  Caddy literal 候选名、numeric UID/GID、Stalwart 实际 storage backend/path 和可靠异地
  SSH/SFTP 通道；官方已确认内置 export/import 必须停服，本审批尚不包含该 quiesce 窗口，
  任一未知都必须 fail closed。
- 创建 `/etc/gsyen-aliyun/prechange-approved` 仍需在快照和文件归档均验证后进行。

## 尚需用户明确回复

需要用户明确批准以下两项后才执行：

- 开通尚未开通的快照服务，并为系统盘 `d-2ze9t48edu0hojhpho4q` 创建一个手工标准快照，预算上限
  `14.80 元/月`，保留到另行确认删除；
- 快照成功后，制作加密的文件级离线备份并验证文件清单、哈希和隔离恢复；只读目录
  盘点已经完成，不需要为盘点本身重复授权。

没有明确批准即维持冻结。
