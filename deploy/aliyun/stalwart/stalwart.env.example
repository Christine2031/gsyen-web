# The config filename/format is version-specific. Set it only after the exact
# pinned candidate and migrated configuration pass an isolated compatibility test.
STALWART_CONFIG_PATH=/srv/gsyen/config/stalwart/__REQUIRED_REVIEWED_CONFIG_FILE__
