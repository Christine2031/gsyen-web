# Build-time public configuration only. Pass this file to the Vite build; the
# completed static server does not read it at runtime. Never put secrets here.
VITE_SGSYEN_API_URL=https://sg-api.gsyen.example.invalid
